name: New MCP server
version: 0.0.1
schema: v1
mcpServers:
{
  "mcpServers": {
    "File System": {
      "command": "npx -y @bunas/fs-mcp@latest",
      "args": [],
      "env": {
        "API_KEY": ""
      }
    }
  }
}